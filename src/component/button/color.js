// The semantic color tokens shared across `packages/pixel/tokens/color.css`
// — the same set `kb-icon` and `kb-text` also resolve their `color`
// attribute against, even without validating it today.
export const COLORS = Object.freeze({
  MASTER: 'master',
  PRIMARY: 'primary',
  COMPLETE: 'complete',
  SUCCESS: 'success',
  WARNING: 'warning',
  DANGER: 'danger',
  INFO: 'info',
  MENU: 'menu',
})
